Add the following files to ~/bin/dotfiles


Add the following into your ~/.bashrc

if [ -f ~/bin/dotfiles/bashrc ]; then
    . ~/bin/dotfiles/bashrc
fi

Link vimrc with
$ ln -s ~/bin/dotfiles/vimrc ~/.vimrc
